Check the contents of a directory.
Returns a bullet point list of all files and directories found inside a directory.
Unlike the other file tools, the path is not restricted to the project root, so
this can also be used to browse locations such as `~/ttyphoon/.images`.