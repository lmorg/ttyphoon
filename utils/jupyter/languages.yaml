- Aliases: [ go, golang ]
  Description: Go (Golang)
  Template: |-
    {{ if begins "package" }}
    {{ .Code }}
    {{ else }}
    package main

    func main() {
    {{ .Code }}
    }
    {{ end }}
  FileExtension: go
  PreRunCommand: [ goimports, -w, "${FILE}" ]
  ExecuteCommand: [ go, run, "${FILE}" ]
  ExecuteParameters: [ go, run, "${FILE}", "${PARAMETERS}"]

- Aliases: [ go, golang, tinygo ]
  Description: Go (TinyGo)
  Template: |-
    {{ if begins "package" }}
    {{ .Code }}
    {{ else }}
    package main

    func main() {
    {{ .Code }}
    }
    {{ end }}
  FileExtension: go
  ExecuteCommand: [ tinygo, run, "${FILE}" ]
  ExecuteParameters: [ tinygo, run, "${FILE}", "${PARAMETERS}"]

- Aliases: [ python, py, python3 ]
  Description: Python 3.x
  Template: |-
    {{ .Code }}
  FileExtension: py
  ExecuteCommand: [ python3, "${FILE}" ]
  ExecuteParameters: [ python3, "${FILE}", "${PARAMETERS}"]

- Aliases: [ python2 ]
  Description: Python 2
  Template: |-
    {{ .Code }}
  FileExtension: py
  ExecuteCommand: [ python2, "${FILE}" ]
  ExecuteParameters: [ python2, "${FILE}", "${PARAMETERS}"]

- Aliases: [ python, py, python3 ]
  Description: Python (PyPy)
  Template: |-
    {{ .Code }}
  FileExtension: py
  ExecuteCommand: [ pypy3, "${FILE}" ]
  ExecuteParameters: [ pypy3, "${FILE}", "${PARAMETERS}"]

- Aliases: [ python, py, python3 ]
  Description: Python (IPython)
  Template: |-
    {{ .Code }}
  FileExtension: py
  ExecuteCommand: [ ipython, "${FILE}" ]
  ExecuteParameters: [ ipython, "${FILE}", "${PARAMETERS}"]

- Aliases: [ bash, sh, shell ]
  Description: Bash Shell Script
  Template: |-
    {{ .Code }}
  FileExtension: sh
  ExecuteCommand: [ bash, "${FILE}" ]
  ExecuteParameters: [ bash, "${FILE}", "${PARAMETERS}"]

- Aliases: [ sh, shell ]
  Description: POSIX Shell (sh)
  Template: |-
    {{ .Code }}
  FileExtension: sh
  ExecuteCommand: [ sh, "${FILE}" ]
  ExecuteParameters: [ sh, "${FILE}", "${PARAMETERS}"]

- Aliases: [ sh, shell, dash ]
  Description: Dash Shell Script
  Template: |-
    {{ .Code }}
  FileExtension: sh
  ExecuteCommand: [ dash, "${FILE}" ]
  ExecuteParameters: [ dash, "${FILE}", "${PARAMETERS}"]

- Aliases: [ murex ]
  Description: Murex Shell Script
  Template: |-
    {{ .Code }}
  FileExtension: mx
  ExecuteCommand: [ murex, "${FILE}" ]
  ExecuteParameters: [ murex, "${FILE}", "${PARAMETERS}"]

- Aliases: [ powershell, pwsh, ps ]
  Description: PowerShell (pwsh)
  Template: |-
    {{ .Code }}
  FileExtension: ps1
  ExecuteCommand: [ pwsh, -NoProfile, -File, "${FILE}" ]
  ExecuteParameters: [ pwsh, -NoProfile, -File, "${FILE}", "${PARAMETERS}"]

- Aliases: [ powershell, pwsh, ps ]
  Description: PowerShell (Windows PowerShell)
  Template: |-
    {{ .Code }}
  FileExtension: ps1
  ExecuteCommand: [ powershell.exe, -NoProfile, -File, "${FILE}" ]
  ExecuteParameters: [ powershell.exe, -NoProfile, -File, "${FILE}", "${PARAMETERS}"]

- Aliases: [ zsh ]
  Description: Zsh Shell Script
  Template: |-
    {{ .Code }}
  FileExtension: zsh
  ExecuteCommand: [ zsh, "${FILE}" ]
  ExecuteParameters: [ zsh, "${FILE}", "${PARAMETERS}"]

- Aliases: [ fish ]
  Description: Fish Shell Script
  Template: |-
    {{ .Code }}
  FileExtension: fish
  ExecuteCommand: [ fish, "${FILE}" ]
  ExecuteParameters: [ fish, "${FILE}", "${PARAMETERS}"]

- Aliases: [ nu, nushell ]
  Description: Nushell
  Template: |-
    {{ .Code }}
  FileExtension: nu
  ExecuteCommand: [ nu, "${FILE}" ]
  ExecuteParameters: [ nu, "${FILE}", "${PARAMETERS}"]

- Aliases: [ dockerfile, docker ]
  Description: Dockerfile (build and run)
  Template: |-
    {{ .Code }}
  FileExtension: Dockerfile
  PreRunCommand: [ docker, build, -t, ttyphoon-jupyter:latest, -f, "${FILE}", "${DIR}" ]
  ExecuteCommand: [ docker, run, --rm, ttyphoon-jupyter:latest ]
  ExecuteParameters: [ docker, run, --rm, ttyphoon-jupyter:latest, "${PARAMETERS}"]

- Aliases: [ ruby, rb ]
  Description: Ruby
  Template: |-
    {{ .Code }}
  FileExtension: rb
  ExecuteCommand: [ ruby, "${FILE}" ]
  ExecuteParameters: [ ruby, "${FILE}", "${PARAMETERS}"]

- Aliases: [ ruby, rb ]
  Description: Ruby (JRuby)
  Template: |-
    {{ .Code }}
  FileExtension: rb
  ExecuteCommand: [ jruby, "${FILE}" ]
  ExecuteParameters: [ jruby, "${FILE}", "${PARAMETERS}"]

- Aliases: [ ruby, rb ]
  Description: Ruby (TruffleRuby)
  Template: |-
    {{ .Code }}
  FileExtension: rb
  ExecuteCommand: [ truffleruby, "${FILE}" ]
  ExecuteParameters: [ truffleruby, "${FILE}", "${PARAMETERS}"]

- Aliases: [ node, js, javascript ]
  Description: Javascript (node.js)
  Template: |-
    {{ .Code }}
  FileExtension: js
  ExecuteCommand: [ node, "${FILE}" ]
  ExecuteParameters: [ node, "${FILE}", "${PARAMETERS}"]

- Aliases: [ node, js, javascript ]
  Description: Javascript (Deno)
  Template: |-
    {{ .Code }}
  FileExtension: js
  ExecuteCommand: [ deno, run, "${FILE}" ]
  ExecuteParameters: [ deno, run, "${FILE}", --, "${PARAMETERS}"]

- Aliases: [ node, js, javascript ]
  Description: Javascript (Bun)
  Template: |-
    {{ .Code }}
  FileExtension: js
  ExecuteCommand: [ bun, "${FILE}" ]
  ExecuteParameters: [ bun, "${FILE}", "${PARAMETERS}"]

- Aliases: [ kotlin, kt ]
  Description: Kotlin (JVM)
  Template: |-
    {{ if contains "fun main" }}
    {{ .Code }}
    {{ else }}
    fun main() {
        {{ .Code }}
    }
    {{ end }}
  FileExtension: kt
  PreRunCommand: [ kotlinc, "${FILE}", -include-runtime, -d, "${FILE}.jar" ]
  ExecuteCommand: [ java, -jar, "${FILE}.jar" ]
  ExecuteParameters: [ java, -jar, "${FILE}.jar", "${PARAMETERS}"]

- Aliases: [ kts, "kotlin script" ]
  Description: Kotlin Script
  Template: |-
    {{ .Code }}
  FileExtension: kts
  ExecuteCommand: [ kotlinc, -script, "${FILE}" ]
  ExecuteParameters: [ kotlinc, -script, "${FILE}", "${PARAMETERS}"]

- Aliases: [ swift ]
  Description: Swift
  Template: |-
    {{ .Code }}
  FileExtension: swift
  ExecuteCommand: [ swift, "${FILE}" ]
  ExecuteParameters: [ swift, "${FILE}", "${PARAMETERS}"]

- Aliases: [ swift ]
  Description: Swift (swiftc)
  Template: |-
    {{ .Code }}
  FileExtension: swift
  PreRunCommand: [ swiftc, "${FILE}", -o, "${FILE}.out" ]
  ExecuteCommand: [ "${FILE}.out" ]
  ExecuteParameters: [ "${FILE}.out", "${PARAMETERS}"]

- Aliases: [ clojure, clj ]
  Description: Clojure
  Template: |-
    {{ .Code }}
  FileExtension: clj
  ExecuteCommand: [ clojure, "${FILE}" ]
  ExecuteParameters: [ clojure, "${FILE}", "${PARAMETERS}"]

- Aliases: [ elixir, ex ]
  Description: Elixir
  Template: |-
    {{ .Code }}
  FileExtension: exs
  ExecuteCommand: [ elixir, "${FILE}" ]
  ExecuteParameters: [ elixir, "${FILE}", "${PARAMETERS}"]

- Aliases: [ erlang, erl ]
  Description: Erlang (escript)
  Template: |-
    {{ if contains "-module" }}
    {{ .Code }}
    {{ else }}
    -module(main).
    -export([main/1]).

    main(_Args) ->
        {{ .Code }}.
    {{ end }}
  FileExtension: escript
  ExecuteCommand: [ escript, "${FILE}" ]
  ExecuteParameters: [ escript, "${FILE}", "${PARAMETERS}"]

- Aliases: [ prolog, plg ]
  Description: Prolog (SWI-Prolog)
  Template: |-
    {{ .Code }}
  FileExtension: pl
  ExecuteCommand: [ swipl, -q, -f, "${FILE}", -t, halt ]
  ExecuteParameters: [ swipl, -q, -f, "${FILE}", -t, halt, "${PARAMETERS}"]

- Aliases: [ julia, jl ]
  Description: Julia
  Template: |-
    {{ .Code }}
  FileExtension: jl
  ExecuteCommand: [ julia, "${FILE}" ]
  ExecuteParameters: [ julia, "${FILE}", "${PARAMETERS}"]

- Aliases: [ lisp, commonlisp, cl ]
  Description: Common Lisp (SBCL)
  Template: |-
    {{ .Code }}
  FileExtension: lisp
  ExecuteCommand: [ sbcl, --script, "${FILE}" ]
  ExecuteParameters: [ sbcl, --script, "${FILE}", "${PARAMETERS}"]

- Aliases: [ lisp, commonlisp, cl ]
  Description: Common Lisp (CLISP)
  Template: |-
    {{ .Code }}
  FileExtension: lisp
  ExecuteCommand: [ clisp, "${FILE}" ]
  ExecuteParameters: [ clisp, "${FILE}", "${PARAMETERS}"]

- Aliases: [ scheme, scm, racket ]
  Description: Scheme (Racket)
  Template: |-
    {{ if begins "#lang" }}
    {{ .Code }}
    {{ else }}
    #lang racket
    {{ .Code }}
    {{ end }}
  FileExtension: rkt
  ExecuteCommand: [ racket, "${FILE}" ]
  ExecuteParameters: [ racket, "${FILE}", "${PARAMETERS}"]

- Aliases: [ scheme, scm ]
  Description: Scheme (Guile)
  Template: |-
    {{ .Code }}
  FileExtension: scm
  ExecuteCommand: [ guile, "${FILE}" ]
  ExecuteParameters: [ guile, "${FILE}", "${PARAMETERS}"]

- Aliases: [ haskell, hs ]
  Description: Haskell
  Template: |-
    {{ if begins "main" }}
    {{ .Code }}
    {{ else }}
    main = do
        {{ .Code }}
    {{ end }}
  FileExtension: hs
  ExecuteCommand: [ runghc, "${FILE}" ]
  ExecuteParameters: [ runghc, "${FILE}", "${PARAMETERS}"]

- Aliases: [ elm ]
  Description: Elm
  Template: |-
    {{ if begins "module" }}
    {{ .Code }}
    {{ else }}
    module Main exposing (..)

    import Html exposing (text)

    main =
        {{ .Code }}
    {{ end }}
  FileExtension: elm
  PreRunCommand: [ elm, make, "${FILE}", '--output="${FILE}.html"' ]
  ExecuteCommand: [ cat, "${FILE}.html" ]
  ExecuteParameters: [ cat, "${FILE}.html", "${PARAMETERS}"]

- Aliases: [ ocaml, ml ]
  Description: OCaml
  Template: |-
    {{ .Code }}
  FileExtension: ml
  ExecuteCommand: [ ocaml, "${FILE}" ]
  ExecuteParameters: [ ocaml, "${FILE}", "${PARAMETERS}"]

- Aliases: [ scala, sc ]
  Description: Scala
  Template: |-
    {{ if contains "object" }}
    {{ .Code }}
    {{ else }}
    object Main extends App {
        {{ .Code }}
    }
    {{ end }}
  FileExtension: scala
  ExecuteCommand: [ scala, "${FILE}" ]
  ExecuteParameters: [ scala, "${FILE}", "${PARAMETERS}"]

- Aliases: [ scala, sc ]
  Description: Scala (scala-cli)
  Template: |-
    {{ if contains "object" }}
    {{ .Code }}
    {{ else }}
    object Main extends App {
        {{ .Code }}
    }
    {{ end }}
  FileExtension: scala
  ExecuteCommand: [ scala-cli, run, "${FILE}" ]
  ExecuteParameters: [ scala-cli, run, "${FILE}", --, "${PARAMETERS}"]

- Aliases: [ scala, sc, ammonite ]
  Description: Scala (Ammonite)
  Template: |-
    {{ .Code }}
  FileExtension: sc
  ExecuteCommand: [ amm, "${FILE}" ]
  ExecuteParameters: [ amm, "${FILE}", "${PARAMETERS}"]

- Aliases: [ haxe, hx ]
  Description: Haxe
  Template: |-
    {{ if contains "class" }}
    {{ .Code }}
    {{ else }}
    class Main {
        static public function main() {
            {{ .Code }}
        }
    }
    {{ end }}
  FileExtension: hx
  ExecuteCommand: [ haxe, --interp, --main, Main, --run, "${FILE}" ]
  ExecuteParameters: [ haxe, --interp, --main, Main, --run, "${FILE}", "${PARAMETERS}"]

- Aliases: [ dart ]
  Description: Dart
  Template: |-
    {{ if contains "main" }}
    {{ .Code }}
    {{ else }}
    void main() {
        {{ .Code }}
    }
    {{ end }}
  FileExtension: dart
  ExecuteCommand: [ dart, "${FILE}" ]
  ExecuteParameters: [ dart, "${FILE}", "${PARAMETERS}"]

- Aliases: [ groovy, gr ]
  Description: Groovy
  Template: |-
    {{ .Code }}
  FileExtension: groovy
  ExecuteCommand: [ groovy, "${FILE}" ]
  ExecuteParameters: [ groovy, "${FILE}", "${PARAMETERS}"]

- Aliases: [ tcl ]
  Description: Tcl
  Template: |-
    {{ .Code }}
  FileExtension: tcl
  ExecuteCommand: [ tclsh, "${FILE}" ]
  ExecuteParameters: [ tclsh, "${FILE}", "${PARAMETERS}"]

- Aliases: [ awk ]
  Description: Awk
  Template: |-
    {{ .Code }}
  FileExtension: awk
  ExecuteCommand: [ awk, -f, "${FILE}" ]
  ExecuteParameters: [ awk, -f, "${FILE}", "${PARAMETERS}"]

- Aliases: [ typescript, ts ]
  Description: TypeScript (ts-node)
  Template: |-
    {{ .Code }}
  FileExtension: ts
  ExecuteCommand: [ ts-node, "${FILE}" ]
  ExecuteParameters: [ ts-node, "${FILE}", "${PARAMETERS}"]

- Aliases: [ typescript, ts ]
  Description: TypeScript (tsx)
  Template: |-
    {{ .Code }}
  FileExtension: ts
  ExecuteCommand: [ tsx, "${FILE}" ]
  ExecuteParameters: [ tsx, "${FILE}", "${PARAMETERS}"]

- Aliases: [ typescript, ts ]
  Description: TypeScript (Deno)
  Template: |-
    {{ .Code }}
  FileExtension: ts
  ExecuteCommand: [ deno, run, "${FILE}" ]
  ExecuteParameters: [ deno, run, "${FILE}", "${PARAMETERS}"]

- Aliases: [ perl, perl5 ]
  Description: Perl 5.x
  Template: |-
    {{ .Code }}
  FileExtension: pl
  ExecuteCommand: [ perl, "${FILE}" ]
  ExecuteParameters: [ perl, "${FILE}", "${PARAMETERS}"]

- Aliases: [ perl, perl5, raku, perl6 ]
  Description: Raku (formerly Perl 6)
  Template: |-
    {{ .Code }}
  FileExtension: raku
  ExecuteCommand: [ raku, "${FILE}" ]
  ExecuteParameters: [ raku, "${FILE}", "${PARAMETERS}"]

- Aliases: [ java ]
  Description: Java
  Template: |-
    {{ if contains "class" }}
    {{ .Code }}
    {{ else }}
    public class Main {
        public static void main(String[] args) {
            {{ .Code }}
        }
    }
    {{ end }}
  FileExtension: java
  PreRunCommand: [ javac, "${FILE}" ]
  ExecuteCommand: [ java, -cp, "${DIR}", Main ]
  ExecuteParameters: [ java, -cp, "${DIR}", Main, "${PARAMETERS}"]

- Aliases: [ java, jshell ]
  Description: Java (JShell)
  Template: |-
    {{ .Code }}
  FileExtension: jsh
  ExecuteCommand: [ jshell, "${FILE}" ]
  ExecuteParameters: [ jshell, "${FILE}", "${PARAMETERS}"]

- Aliases: [ objective-c, objc, obj-c ]
  Description: Objective-C
  Template: |-
    {{ if begins "#import" }}
    {{ .Code }}
    {{ else }}
    #import <Foundation/Foundation.h>

    int main(int argc, const char * argv[]) {
        @autoreleasepool {
            {{ .Code }}
        }
        return 0;
    }
    {{ end }}
  FileExtension: m
  PreRunCommand: [ clang, -framework, Foundation, "${FILE}", -o, "${FILE}.out" ]
  ExecuteCommand: [ "${FILE}.out" ]
  ExecuteParameters: [ "${FILE}.out", "${PARAMETERS}"]

- Aliases: [ d ]
  Description: D
  Template: |-
    {{ if begins "import" }}
    {{ .Code }}
    {{ else }}
    import std.stdio;

    void main() {
        {{ .Code }}
    }
    {{ end }}
  FileExtension: d
  PreRunCommand: [ dmd, "${FILE}", "-of=${FILE}.out" ]
  ExecuteCommand: [ "${FILE}.out" ]
  ExecuteParameters: [ "${FILE}.out", "${PARAMETERS}"]

- Aliases: [ d ]
  Description: D (LDC2)
  Template: |-
    {{ if begins "import" }}
    {{ .Code }}
    {{ else }}
    import std.stdio;

    void main() {
        {{ .Code }}
    }
    {{ end }}
  FileExtension: d
  PreRunCommand: [ ldc2, "${FILE}", "-of=${FILE}.out" ]
  ExecuteCommand: [ "${FILE}.out" ]
  ExecuteParameters: [ "${FILE}.out", "${PARAMETERS}"]

- Aliases: [ d ]
  Description: D (GDC)
  Template: |-
    {{ if begins "import" }}
    {{ .Code }}
    {{ else }}
    import std.stdio;

    void main() {
        {{ .Code }}
    }
    {{ end }}
  FileExtension: d
  PreRunCommand: [ gdc, "${FILE}", -o, "${FILE}.out" ]
  ExecuteCommand: [ "${FILE}.out" ]
  ExecuteParameters: [ "${FILE}.out", "${PARAMETERS}"]

- Aliases: [ c ]
  Description: C (cc)
  Template: |-
    {{ if begins "#include" }}
    {{ .Code }}
    {{ else }}
    #include <stdio.h>

    int main(void) {
        {{ .Code }}
        return 0;
    }
    {{ end }}
  FileExtension: c
  PreRunCommand: [ cc, "${FILE}", -o, "${FILE}.out" ]
  ExecuteCommand: [ "${FILE}.out" ]
  ExecuteParameters: [ "${FILE}.out", "${PARAMETERS}"]

- Aliases: [ c ]
  Description: C (gcc)
  Template: |-
    {{ if begins "#include" }}
    {{ .Code }}
    {{ else }}
    #include <stdio.h>

    int main(void) {
        {{ .Code }}
        return 0;
    }
    {{ end }}
  FileExtension: c
  PreRunCommand: [ gcc, "${FILE}", -o, "${FILE}.out" ]
  ExecuteCommand: [ "${FILE}.out" ]
  ExecuteParameters: [ "${FILE}.out", "${PARAMETERS}"]

- Aliases: [ c ]
  Description: C (clang)
  Template: |-
    {{ if begins "#include" }}
    {{ .Code }}
    {{ else }}
    #include <stdio.h>

    int main(void) {
        {{ .Code }}
        return 0;
    }
    {{ end }}
  FileExtension: c
  PreRunCommand: [ clang, "${FILE}", -o, "${FILE}.out" ]
  ExecuteCommand: [ "${FILE}.out" ]
  ExecuteParameters: [ "${FILE}.out", "${PARAMETERS}"]

- Aliases: [ c ]
  Description: C (MSVC)
  Template: |-
    {{ if begins "#include" }}
    {{ .Code }}
    {{ else }}
    #include <stdio.h>

    int main(void) {
        {{ .Code }}
        return 0;
    }
    {{ end }}
  FileExtension: c
  PreRunCommand: [ cl.exe, /nologo, "${FILE}", "/Fe:${FILE}.exe" ]
  ExecuteCommand: [ "${FILE}.exe" ]
  ExecuteParameters: [ "${FILE}.exe", "${PARAMETERS}"]

- Aliases: [ cpp, c++, cxx ]
  Description: C++ (c++)
  Template: |-
    {{ if begins "#include" }}
    {{ .Code }}
    {{ else }}
    #include <iostream>

    int main() {
        {{ .Code }}
        return 0;
    }
    {{ end }}
  FileExtension: cpp
  PreRunCommand: [ c++, "${FILE}", -o, "${FILE}.out" ]
  ExecuteCommand: [ "${FILE}.out" ]
  ExecuteParameters: [ "${FILE}.out", "${PARAMETERS}"]

- Aliases: [ cpp, c++, cxx ]
  Description: C++ (g++)
  Template: |-
    {{ if begins "#include" }}
    {{ .Code }}
    {{ else }}
    #include <iostream>

    int main() {
        {{ .Code }}
        return 0;
    }
    {{ end }}
  FileExtension: cpp
  PreRunCommand: [ g++, "${FILE}", -o, "${FILE}.out" ]
  ExecuteCommand: [ "${FILE}.out" ]
  ExecuteParameters: [ "${FILE}.out", "${PARAMETERS}"]

- Aliases: [ cpp, c++, cxx ]
  Description: C++ (clang++)
  Template: |-
    {{ if begins "#include" }}
    {{ .Code }}
    {{ else }}
    #include <iostream>

    int main() {
        {{ .Code }}
        return 0;
    }
    {{ end }}
  FileExtension: cpp
  PreRunCommand: [ clang++, "${FILE}", -o, "${FILE}.out" ]
  ExecuteCommand: [ "${FILE}.out" ]
  ExecuteParameters: [ "${FILE}.out", "${PARAMETERS}"]

- Aliases: [ cpp, c++, cxx ]
  Description: C++ (MSVC)
  Template: |-
    {{ if begins "#include" }}
    {{ .Code }}
    {{ else }}
    #include <iostream>

    int main() {
        {{ .Code }}
        return 0;
    }
    {{ end }}
  FileExtension: cpp
  PreRunCommand: [ cl.exe, /nologo, /EHsc, "${FILE}", "/Fe:${FILE}.exe" ]
  ExecuteCommand: [ "${FILE}.exe" ]
  ExecuteParameters: [ "${FILE}.exe", "${PARAMETERS}"]

- Aliases: [ rust, rs ]
  Description: Rust
  Template: |-
    {{ if begins "fn main" }}
    {{ .Code }}
    {{ else }}
    fn main() {
        {{ .Code }}
    }
    {{ end }}
  FileExtension: rs
  PreRunCommand: [ rustc, "${FILE}", -o, "${FILE}.out" ]
  ExecuteCommand: [ "${FILE}.out" ]
  ExecuteParameters: [ "${FILE}.out", "${PARAMETERS}"]

- Aliases: [ crystal, cr ]
  Description: Crystal
  Template: |-
    {{ .Code }}
  FileExtension: cr
  ExecuteCommand: [ crystal, run, "${FILE}" ]
  ExecuteParameters: [ crystal, run, "${FILE}", --, "${PARAMETERS}"]

- Aliases: [ vlang, v ]
  Description: V
  Template: |-
    {{ if contains "fn main" }}
    {{ .Code }}
    {{ else }}
    fn main() {
        {{ .Code }}
    }
    {{ end }}
  FileExtension: v
  ExecuteCommand: [ v, run, "${FILE}" ]
  ExecuteParameters: [ v, run, "${FILE}", "${PARAMETERS}"]

- Aliases: [ zig ]
  Description: Zig
  Template: |-
    {{ if contains "pub fn main" }}
    {{ .Code }}
    {{ else }}
    const std = @import("std");

    pub fn main() !void {
        {{ .Code }}
    }
    {{ end }}
  FileExtension: zig
  ExecuteCommand: [ zig, run, "${FILE}" ]
  ExecuteParameters: [ zig, run, "${FILE}", --, "${PARAMETERS}"]

- Aliases: [ odin ]
  Description: Odin
  Template: |-
    {{ if contains "package" }}
    {{ .Code }}
    {{ else }}
    package main

    import "core:fmt"

    main :: proc() {
        {{ .Code }}
    }
    {{ end }}
  FileExtension: odin
  ExecuteCommand: [ odin, run, "${FILE}" ]
  ExecuteParameters: [ odin, run, "${FILE}", "${PARAMETERS}"]

- Aliases: [ red ]
  Description: Red
  Template: |-
    {{ if begins "Red" }}
    {{ .Code }}
    {{ else }}
    Red []

    {{ .Code }}
    {{ end }}
  FileExtension: red
  ExecuteCommand: [ red, "${FILE}" ]
  ExecuteParameters: [ red, "${FILE}", "${PARAMETERS}"]

- Aliases: [ nim ]
  Description: Nim
  Template: |-
    {{ if contains "when isMainModule" }}
    {{ .Code }}
    {{ else }}
    when isMainModule:
      {{ .Code }}
    {{ end }}
  FileExtension: nim
  PreRunCommand: [ nim, c, "-o:${FILE}.out", "${FILE}" ]
  ExecuteCommand: [ "${FILE}.out" ]
  ExecuteParameters: [ "${FILE}.out", "${PARAMETERS}"]

- Aliases: [ fortran, f90, f95, f03, f08 ]
  Description: Fortran
  Template: |-
    {{ if contains "program" }}
    {{ .Code }}
    {{ else }}
    program main
      {{ .Code }}
    end program main
    {{ end }}
  FileExtension: f90
  PreRunCommand: [ gfortran, "${FILE}", -o, "${FILE}.out" ]
  ExecuteCommand: [ "${FILE}.out" ]
  ExecuteParameters: [ "${FILE}.out", "${PARAMETERS}"]

- Aliases: [ fortran, f90, f95, f03, f08 ]
  Description: Fortran (Flang)
  Template: |-
    {{ if contains "program" }}
    {{ .Code }}
    {{ else }}
    program main
      {{ .Code }}
    end program main
    {{ end }}
  FileExtension: f90
  PreRunCommand: [ flang, "${FILE}", -o, "${FILE}.out" ]
  ExecuteCommand: [ "${FILE}.out" ]
  ExecuteParameters: [ "${FILE}.out", "${PARAMETERS}"]

- Aliases: [ fortran, f90, f95, f03, f08 ]
  Description: Fortran (Intel ifx)
  Template: |-
    {{ if contains "program" }}
    {{ .Code }}
    {{ else }}
    program main
      {{ .Code }}
    end program main
    {{ end }}
  FileExtension: f90
  PreRunCommand: [ ifx, "${FILE}", -o, "${FILE}.out" ]
  ExecuteCommand: [ "${FILE}.out" ]
  ExecuteParameters: [ "${FILE}.out", "${PARAMETERS}"]

- Aliases: [ cobol, cob, cbl ]
  Description: COBOL (GnuCOBOL)
  Template: |-
    {{ .Code }}
  FileExtension: cbl
  PreRunCommand: [ cobc, -x, "${FILE}", -o, "${FILE}.out" ]
  ExecuteCommand: [ "${FILE}.out" ]
  ExecuteParameters: [ "${FILE}.out", "${PARAMETERS}"]

- Aliases: [ php ]
  Description: PHP
  Template: |-
    {{ if begins "<?php" }}
    {{ .Code }}
    {{ else }}
    <?php
    {{ .Code }}
    {{ end }}
  FileExtension: php
  ExecuteCommand: [ php, "${FILE}" ]
  ExecuteParameters: [ php, "${FILE}", "${PARAMETERS}"]

- Aliases: [ php ]
  Description: PHP (PHP-CGI)
  Template: |-
    {{ if begins "<?php" }}
    {{ .Code }}
    {{ else }}
    <?php
    {{ .Code }}
    {{ end }}
  FileExtension: php
  ExecuteCommand: [ php-cgi, "${FILE}" ]
  ExecuteParameters: [ php-cgi, "${FILE}", "${PARAMETERS}"]

- Aliases: [ php ]
  Description: PHP (HHVM)
  Template: |-
    {{ if begins "<?php" }}
    {{ .Code }}
    {{ else }}
    <?php
    {{ .Code }}
    {{ end }}
  FileExtension: php
  ExecuteCommand: [ hhvm, "${FILE}" ]
  ExecuteParameters: [ hhvm, "${FILE}", "${PARAMETERS}"]

- Aliases: [ lua ]
  Description: Lua
  Template: |-
    {{ .Code }}
  FileExtension: lua
  ExecuteCommand: [ lua, "${FILE}" ]
  ExecuteParameters: [ lua, "${FILE}", "${PARAMETERS}"]

- Aliases: [ r, rstats ]
  Description: R
  Template: |-
    {{ .Code }}
  FileExtension: r
  ExecuteCommand: [ Rscript, "${FILE}" ]
  ExecuteParameters: [ Rscript, "${FILE}", "${PARAMETERS}"]

- Aliases: [ csharp, cs, dotnet ]
  Description: C# (.NET)
  Template: |-
    {{ .Code }}
  FileExtension: cs
  ExecuteCommand: [ dotnet, script, "${FILE}" ]
  ExecuteParameters: [ dotnet, script, "${FILE}", --, "${PARAMETERS}"]

- Aliases: [ forth, fth ]
  Description: Forth (Gforth)
  Template: |-
    {{ .Code }}
  FileExtension: fs
  ExecuteCommand: [ gforth, "${FILE}" ]
  ExecuteParameters: [ gforth, "${FILE}", "${PARAMETERS}"]

- Aliases: [ csharp, cs, dotnet ]
  Description: C# (C# Interactive - csi)
  Template: |-
    {{ .Code }}
  FileExtension: cs
  ExecuteCommand: [ csi, "${FILE}" ]
  ExecuteParameters: [ csi, "${FILE}", "${PARAMETERS}"]

- Aliases: [ fsharp, fs, "f#" ]
  Description: F# (F# Interactive)
  Template: |-
    {{ .Code }}
  FileExtension: fsx
  ExecuteCommand: [ dotnet, fsi, "${FILE}" ]
  ExecuteParameters: [ dotnet, fsi, "${FILE}", --, "${PARAMETERS}"]

- Aliases: [ vb, vbnet, "vb.net" ]
  Description: VB.NET (Mono)
  Template: |-
    {{ if contains "Module" }}
    {{ .Code }}
    {{ else }}
    Module Program
        Sub Main()
            {{ .Code }}
        End Sub
    End Module
    {{ end }}
  FileExtension: vb
  PreRunCommand: [ vbnc, "${FILE}", '-out:"${FILE}.exe"' ]
  ExecuteCommand: [ mono, "${FILE}.exe" ]
  ExecuteParameters: [ mono, "${FILE}.exe", "${PARAMETERS}"]

- Aliases: [ vbscript, vbs ]
  Description: VBScript (Windows)
  Template: |-
    {{ .Code }}
  FileExtension: vbs
  ExecuteCommand: [ cscript.exe, //NoLogo, "${FILE}" ]
  ExecuteParameters: [ cscript.exe, //NoLogo, "${FILE}", "${PARAMETERS}"]

- Aliases: [ vb, vbnet, "vb.net" ]
  Description: VB.NET (.NET SDK)
  Template: |-
    {{ if contains "Module" }}
    {{ .Code }}
    {{ else }}
    Module Program
        Sub Main()
            {{ .Code }}
        End Sub
    End Module
    {{ end }}
  FileExtension: vb
  ExecuteCommand: [ dotnet, script, "${FILE}" ]
  ExecuteParameters: [ dotnet, script, "${FILE}", "${PARAMETERS}"]

- Aliases: [ ada, adb, ads ]
  Description: Ada (GNAT)
  Template: |-
    {{ if begins "with" }}
    {{ .Code }}
    {{ else }}
    with Ada.Text_IO; use Ada.Text_IO;

    procedure Main is
    begin
       {{ .Code }}
    end Main;
    {{ end }}
  FileExtension: adb
  PreRunCommand: [ gnatmake, "${FILE}", -o, "${FILE}.out" ]
  ExecuteCommand: [ "${FILE}.out" ]
  ExecuteParameters: [ "${FILE}.out", "${PARAMETERS}"]

- Aliases: [ ada, adb, ads ]
  Description: Ada (GPRbuild)
  Template: |-
    {{ if begins "with" }}
    {{ .Code }}
    {{ else }}
    with Ada.Text_IO; use Ada.Text_IO;

    procedure Main is
    begin
       {{ .Code }}
    end Main;
    {{ end }}
  FileExtension: adb
  PreRunCommand: [ gprbuild, "${FILE}", -o, "${FILE}.out" ]
  ExecuteCommand: [ "${FILE}.out" ]
  ExecuteParameters: [ "${FILE}.out", "${PARAMETERS}"]

- Aliases: [ smalltalk, st ]
  Description: Smalltalk (GNU Smalltalk)
  Template: |-
    {{ .Code }}
  FileExtension: st
  ExecuteCommand: [ gst, "${FILE}" ]
  ExecuteParameters: [ gst, "${FILE}", "${PARAMETERS}"]

- Aliases: [ wolfram, mathematica, wl ]
  Description: Wolfram Language (wolframscript)
  Template: |-
    {{ .Code }}
  FileExtension: wl
  ExecuteCommand: [ wolframscript, -file, "${FILE}" ]
  ExecuteParameters: [ wolframscript, -file, "${FILE}", "${PARAMETERS}"]

- Aliases: [ wolfram, mathematica, wl ]
  Description: Wolfram Language (MathKernel)
  Template: |-
    {{ .Code }}
  FileExtension: wl
  ExecuteCommand: [ MathKernel, -script, "${FILE}" ]
  ExecuteParameters: [ MathKernel, -script, "${FILE}", "${PARAMETERS}"]

- Aliases: [ delphi, objectpascal, "object-pascal" ]
  Description: Delphi / Object Pascal (Free Pascal)
  Template: |-
    {{ if begins "program" }}
    {{ .Code }}
    {{ else }}
    program Main;
    begin
        {{ .Code }}
    end.
    {{ end }}
  FileExtension: pas
  PreRunCommand: [ fpc, "${FILE}", "-o${FILE}.out" ]
  ExecuteCommand: [ "${FILE}.out" ]
  ExecuteParameters: [ "${FILE}.out", "${PARAMETERS}"]

- Aliases: [ pascal, pas ]
  Description: Pascal (Free Pascal)
  Template: |-
    {{ if begins "program" }}
    {{ .Code }}
    {{ else }}
    program Main;
    begin
        {{ .Code }}
    end.
    {{ end }}
  FileExtension: pas
  PreRunCommand: [ fpc, "${FILE}", "-o${FILE}.out" ]
  ExecuteCommand: [ "${FILE}.out" ]
  ExecuteParameters: [ "${FILE}.out", "${PARAMETERS}"]

- Aliases: [ basic, bas, freebasic ]
  Description: BASIC (FreeBASIC)
  Template: |-
    {{ .Code }}
  FileExtension: bas
  PreRunCommand: [ fbc, "${FILE}", "-x", "${FILE}.out" ]
  ExecuteCommand: [ "${FILE}.out" ]
  ExecuteParameters: [ "${FILE}.out", "${PARAMETERS}"]

- Aliases: [ asm, assembly, gas ]
  Description: Assembly (GNU as via cc)
  Template: |-
    {{ .Code }}
  FileExtension: s
  PreRunCommand: [ cc, "${FILE}", -o, "${FILE}.out" ]
  ExecuteCommand: [ "${FILE}.out" ]
  ExecuteParameters: [ "${FILE}.out", "${PARAMETERS}"]

- Aliases: [ nasm ]
  Description: Assembly (NASM)
  Template: |-
    {{ .Code }}
  FileExtension: asm
  PreRunCommand: [ nasm, -f, elf64, "${FILE}", -o, "${FILE}.o" ]
  PreRunCommand2: [ ld, "${FILE}.o", -o, "${FILE}.out" ]
  ExecuteCommand: [ "${FILE}.out" ]
  ExecuteParameters: [ "${FILE}.out", "${PARAMETERS}"]

- Aliases: [ yasm ]
  Description: Assembly (YASM)
  Template: |-
    {{ .Code }}
  FileExtension: asm
  PreRunCommand: [ yasm, -f, elf64, "${FILE}", -o, "${FILE}.o" ]
  PreRunCommand2: [ ld, "${FILE}.o", -o, "${FILE}.out" ]
  ExecuteCommand: [ "${FILE}.out" ]
  ExecuteParameters: [ "${FILE}.out", "${PARAMETERS}"]

- Aliases: [ fasm ]
  Description: Assembly (FASM)
  Template: |-
    {{ .Code }}
  FileExtension: asm
  PreRunCommand: [ fasm, "${FILE}", "${FILE}.out" ]
  ExecuteCommand: [ "${FILE}.out" ]
  ExecuteParameters: [ "${FILE}.out", "${PARAMETERS}"]

- Aliases: [ goasm, goassembly, "go-asm", "go assembly" ]
  Description: Go Assembly (Plan 9 assembler)
  Template: |-
    {{ .Code }}
  FileExtension: s
  PreRunCommand: [ go, tool, asm, -o, "${FILE}.o", "${FILE}" ]
  ExecuteCommand: [ go, tool, objdump, "${FILE}.o" ]
  ExecuteParameters: [ go, tool, objdump, "${FILE}.o", "${PARAMETERS}"]
